"# MANACO - Landing Page" 
"Soy Franco Medina, estudiante de Sistemas, y este es mi proyecto de landing page responsive para MANACO." 

## Accesibilidad (a11y)
- Añadí un **skip link** (`<a class="skip-link" href="#main">`) para saltar directamente al contenido principal con teclado.
- Todos los elementos interactivos usan enfoque visible (`:focus-visible`) para facilitar la navegación con teclado.
- Imágenes con `alt` descriptivos que comunican la función y apariencia del producto.
- Uso mínimo de `aria-*` y sólo donde aporta (ej. `aria-label` en navegación y `aria-expanded` en el botón del menú).
- Validé contraste de colores para textos y botones 